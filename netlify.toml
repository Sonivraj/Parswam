[build]
  # Use the normal Next build command; Netlify's Next plugin will handle the output
  command = "npm run build"
  # Use the normal build command; the Netlify Next plugin manages the publish output.

[[plugins]]
  package = "@netlify/plugin-nextjs"
